apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: geth-swap
  namespace: private-localstorev2
spec:
  ingressClassName: nginx-internal
  rules:
  - host: geth-swap.private-localstorev2.testnet.internal
    http:
      paths:
      - backend:
          service:
            name: geth-geth-swap
            port:
              number: 8545
        path: /
        pathType: Prefix
